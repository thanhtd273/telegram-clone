import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const SavedMessageScreen = (): JSX.Element => {
  return (
    <View>
      <Text>Saved Message Screen</Text>
    </View>
  );
};

export default SavedMessageScreen;

const styles = StyleSheet.create({
  container: {},
});
